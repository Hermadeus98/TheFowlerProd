using System.Collections;
using System.Collections.Generic;
using QRCode;
using UnityEngine;

namespace TheFowler
{
    public static class DialogueManager
    {
        /*public static Dictionary<string, IDialoguePhase> DialoguePhases = new Dictionary<string, IDialoguePhase>();

        public static void RegisterDialoguePhase(IDialoguePhase dialoguePhase)
        {
            if (!DialoguePhases.ContainsKey(dialoguePhase.Dialogue_id))
            {
                DialoguePhases.Add(dialoguePhase.Dialogue_id, dialoguePhase);
            }
        }
        
        public static void UnregisterDialoguePhase(IDialoguePhase dialoguePhase)
        {
            if (DialoguePhases.ContainsKey(dialoguePhase.Dialogue_id))
            {
                DialoguePhases.Remove(dialoguePhase.Dialogue_id);
            }
        }

        public static void PlayDialoguePhase(string key)
        {
            DialoguePhases[key].PlayDialoguePhase();
        }*/
    }
}
